# 📘 โครงสร้างโปรเจ็ค FaceCheck Web (Frontend)

เอกสารฉบับนี้จัดทำขึ้นเพื่อใช้ **อธิบายโครงสร้างของโปรเจ็ค** ให้อาจารย์ที่ปรึกษาหรือผู้ประเมินเข้าใจว่าโปรเจ็คการเช็คชื่อด้วยใบหน้า (FaceCheck) ในฝั่ง Frontend (React.js) มีการจัดวางไฟล์อย่างไรบ้าง เพื่อความเป็นระเบียบและง่ายต่อการดูแลรักษา (Maintainability)

---

## 📂 โครงสร้างโฟลเดอร์หลัก (Main Structure)

โฟลเดอร์ `src/` เป็นหัวใจหลักของโปรเจ็ค React โดยมีการแบ่งโฟลเดอร์ตามหน้าที่การทำงาน (Separation of Concerns) ดังนี้:

### 1. `pages/` (หน้าจอผู้ใช้งาน - UI Pages)
เป็นโฟลเดอร์ที่เก็บ "หน้าตาของแอปพลิเคชัน" (Views) ที่ผู้ใช้จะมองเห็น โดยแบ่งย่อยเป็น 3 ระบบหลัก:
* **`student/` (ระบบของนักศึกษา):** 
  - `StudentDashboard.jsx`: หน้าหลักของนักศึกษา แสดงภาพรวมวิชาเรียน
  - `StudentCourses.jsx`: หน้าค้นหาและกดยืนยันเข้าร่วมคลาสเรียน
  - `FaceRegistration.jsx`: หน้าต่างสแกนใบหน้าเพื่อลงทะเบียนในระบบ
* **`teacher/` (ระบบของอาจารย์):**
  - `TeacherDashboard.jsx`: แผงควบคุมหลักของอาจารย์ แสดงวิชาที่สอนทั้งหมด
  - `TeacherCourseDetail.jsx`: หน้าจัดการคลาสเรียน (กดสร้างห้อง, กดปุ่มเช็คชื่อ, ดูคนมาเรียน)
* **`auth/` (ระบบเข้าสู่ระบบ):**
  - `Login.jsx`: หน้าเข้าสู่ระบบและตรวจสอบสิทธิ์

### 2. `services/` (ผู้ประสานงาน API - API Connectors)
โฟลเดอร์นี้สำคัญที่สุดในแง่ของ "ตรรกะข้อมูล (Business Logic)" แฟ้มนี้จะรวบรวมฟังก์ชันสำหรับใช้ยิง HTTP Request (GET, POST, PUT, DELETE) ไปคุยกับ **Backend (Spring Boot)**
* `api.js`: ตั้งค่าเริ่มต้น (Axios Instance) เช่น การแปะ Token ยืนยันตัวตนในทุกๆ Request
* `classService.js`: จัดการข้อมูลวิชา (สร้างห้องเรียน, ดึงรายชื่อ, ยกเลิกคลาส)
* `attendanceService.js`: จัดการเรื่องเช็คชื่อ (เรียกดูบันทึก, ดูสถิติการมาเรียน)

> **💡 ทริคสำหรับการพรีเซนต์:** หากอาจารย์ถามว่า *"กดปุ่มนี้แล้วระบบส่งข้อมูลไปฐานข้อมูลยังไง?"* คำตอบคือ *"หน้า UI จะเรียกใช้ฟังก์ชันในโฟลเดอร์ `services` ซึ่งตัว services นี้จะทำหน้าที่ยิงข้อมูลไปหา Backend ฝั่ง Java เพื่อลงฐานข้อมูลอีกทีครับ"*

### 3. `context/` (ความจำส่วนกลาง - Global States)
โดยปกติ React จะจำข้อมูลเป็นหน้าๆ ไป แต่โฟลเดอร์นี้เอาไว้เก็บ **"ข้อมูลที่ทุกคนต้องรู้ร่วมกัน"**
* `AuthContext.jsx`: โค้ดส่วนนี้จะจำว่า **"ใครล็อกอินอยู่"** (เช่น รหัสนักศึกษาอะไร มีสิทธิ์เป็นนักเรียนหรืออาจารย์) หน้าจออื่นๆ ก็แค่มาดึงข้อมูลตัวนี้ไปใช้ ทำให้ไม่ต้องส่งข้อมูลล็อกอินซับซ้อนระหว่างหน้าจอ

### 4. `components/` (ชิ้นส่วนหน้าจอประกอบ - Reusable Components)
ส่วนประกอบของหน้าจอที่สามารถ **"หยิบไปใช้ซ้ำได้หลายๆ หน้า"** 
* `common/`: ของใช้ทั่วไป เช่น `ProtectedRoute.jsx` ที่คอยดักไม่ให้นักศึกษาแอบพิมพ์ URL เข้าหน้าอาจารย์
* `layout/`: กรอบนอกของแอป เช่น `MainLayout.jsx` ที่มีแถบเมนูด้านซ้ายติดไว้ตลอด ไม่ว่าจะเปลี่ยนคลิกไปหน้าไหนก็ตาม

---

## 🚀 ลำดับการทำงาน (Application Flow)

1. **จุดเริ่มต้น (Entry Point):** เมื่อแอปถูกรัน ไฟล์ `main.jsx` จะถูกเรียกก่อนเพื่อน
2. **ระบบนำทาง (Routing):** `App.jsx` จะทำหน้าที่เป็น "ป้ายบอกทาง" ตรวจสอบรหัส URL ว่าควรอ่านและแสดงหน้าเพจ (Pages) จากไฟล์ไหน
3. **ตรวจสอบสิทธิ์ (Auth Check):** ผ่านตัว `ProtectedRoute` และ `AuthContext` 
4. **แสดงผล (Rendering):** เมื่อโหลดหน้าเพจเสร็จ หน้าเพจนั้นๆ อาจจะยืมชิ้นส่วนจาก `components/` มาจัดเรียง และถ้าจำเป็นต้องดึงข้อมูลจาก Server ก็จะสั่งให้ไฟล์ใน `services/` ไปเอาข้อมูลมาแสดงให้ผู้ใช้เห็น

---
*จัดทำขึ้นเพื่อนำเสนอโครงสร้าง Clean Code Architecture ของโปรเจ็ค*
